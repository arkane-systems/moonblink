[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "moonblink"
version = "0.1.0"
description = "Moonraker/Klipper Blinkt! status display service"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["moonblink"]

[tool.ruff]
line-length = 140
